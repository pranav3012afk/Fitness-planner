# Fitness-planner
The AI Fitness Planner is a React-based web application that generates personalized fitness and diet plans using Google Gemini AI. User inputs are sent directly from the browser to Gemini’s cloud backend, which processes the data and returns an AI-generated plan for display
